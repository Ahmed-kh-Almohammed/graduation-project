'use strict';

/**
 * patient-profile controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::patient-profile.patient-profile');
